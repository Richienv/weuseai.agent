{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent bundle manifest",
  "description": "Per-agent inventory of skills + templates shipped to customer's VPS at provision time. Validated by supabase/functions/_shared/manifest-validator.ts at provisioning + at test time (drift checks).",
  "type": "object",
  "required": ["agent_slug", "version", "description_id", "skills", "templates", "self_extend", "extension_dir"],
  "properties": {
    "$schema": { "type": "string" },
    "agent_slug": {
      "type": "string",
      "description": "Folder slug — must match one of the 10 known PERSONA_SLUGS in soul-md-template.ts"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semver. Bumped when skills[] or templates[] change."
    },
    "description_id": {
      "type": "string",
      "minLength": 10,
      "maxLength": 500,
      "description": "Bahasa Indonesia bundle description for catalog UI"
    },
    "skills": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "description_id", "execution", "handler_ref", "tier"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]+$",
            "description": "kebab-case skill identifier"
          },
          "description_id": {
            "type": "string",
            "minLength": 10,
            "maxLength": 500
          },
          "description_en": {
            "type": "string",
            "minLength": 10,
            "maxLength": 500
          },
          "skill_kind": {
            "type": "string",
            "enum": ["skill", "playbook"],
            "description": "Discriminates a single-shot skill ('skill', the default when absent) from a multi-step playbook ('playbook' — SKILL.md carries a ## Langkah-langkah section of ordered gated steps, driven by the flow-state state-machine engine)."
          },
          "templates_used": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Template ids (from this manifest's templates[]) the skill references"
          },
          "execution": {
            "type": "string",
            "enum": ["edge-function", "hermes-skill", "composite", "external-api"]
          },
          "handler_ref": {
            "type": "string",
            "pattern": "^(edge-fn|hermes-skill|external|composite):[a-z0-9-]+$"
          },
          "tier": {
            "type": "string",
            "enum": ["starter", "pro", "studio"]
          }
        }
      }
    },
    "templates": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "kind", "description_id"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "kind": {
            "type": "string",
            "description": "Free-form category label: invoice, contract, letter, fixture, schema-spec, etc."
          },
          "description_id": {
            "type": "string",
            "minLength": 10,
            "maxLength": 500
          },
          "best_for": { "type": "string" },
          "source": {
            "type": "string",
            "enum": ["bundled", "customer-grown"],
            "description": "bundled = shipped from agent-packs/; customer-grown = generated by extend-capabilities"
          },
          "generated_at": {
            "type": "string",
            "description": "ISO8601 timestamp; only present when source=customer-grown"
          }
        }
      }
    },
    "self_extend": {
      "type": "boolean",
      "description": "True iff this agent's runtime can use the extend-capabilities skill to grow its library at runtime"
    },
    "extension_dir": {
      "type": "string",
      "pattern": "^/",
      "description": "Absolute path on the customer's VPS where customer-grown templates persist"
    }
  }
}
