# Landing QA + conversion findings (2026-06-17)

## P0 — must fix (broken, dishonest, gate-risk, or kills conversion)

- `[honesty/gate]` assets/app.jsx:5178-5183 — School Expert ships `status:'live'` with a pulsing "Aktif" chip + past-tense `msg` "Tugas dari portal sekolah kamu sudah aku rapikan" — directly contradicts the section's own founder-confirmed comment (5152-5157: only R2 Content/Fit/Finance live, "Gmail/Sekolah is Segera"); this is a fabricated live capability + an animated lie — set `status:'segera'`, keep `line:'Pantau tugas & tenggat sekolah.'`, rewrite `msg` to future tense ("Nanti aku bantu rapikan tugas dari portal sekolah kamu — checklist dan tenggat siap.").
- `[a11y]` assets/app.jsx:2867-2880, 2908 — 4 real focusable `<button>`s in the fake dashboard do nothing (no onClick; db-send even announces "Kirim pesan") so keyboard/SR users tab into dead controls — add `tabIndex={-1}` + `aria-hidden="true"` to each (and drop the db-send `aria-label`).
- `[perf]` index.html:39 — `<link rel="preload" as="video" href="assets/new-hero.mp4">` force-downloads 1.32 MB at top priority for a video only sampled for metadata by DottedVideo (never displayed), starving real LCP on 4G Android — delete the preload line; DottedVideo's `preload="metadata"` fetches lazily.
- `[css/visual]` index.html:333,357 — `.hero-circuit { z-index:2 }` renders the red wires ON TOP of `.db-hero-dim { z-index:1 }`, inverting the documented "Sits behind .db-hero-dim" intent (332) so wires punch through the contrast scrim — set `.hero-circuit { z-index:1 }`.
- `[css/visual]` index.html:528 + assets/app.jsx:5188,5191 — `.live-dot` is styled ONLY under `.db-section .db-eyebrow-pill .live-dot`, so the `<span className="live-dot">` in IntegrationsSpine's `.is-eyebrow`/`.is-hub` is a zero-size invisible dot leaving a phantom gap — add a generic `.live-dot{width:5px;height:5px;border-radius:50%;background:#E5322D}` (RM-guard its animation).
- `[conversion]` assets/app.jsx:2846-2851 — two competing above-fold CTAs ("Aktifkan asisten kamu" + "Konsultasi gratis 15 menit") add a decision for a 1-second reel viewer — keep ONE primary above fold, demote the consult CTA to a text link (the 2-CTA pair lower down at 5321-5333 is fine).
- `[conversion]` assets/app.jsx:5371-5385 — render order puts a SECOND full hero (OriginSection: own headline/sub/4-stat counter/duplicate CTA pair) + a 12-card carousel between the hero and any new value, churning IG viewers — compress OriginSection to a slim credibility strip (badge + one line + 4 stats, no duplicate headline/CTA) and move Pricing earlier (hero → 4 apps → price).

## P1 — should fix (clear quality/clarity/a11y/perf wins)

- `[voice]` assets/app.jsx:4669-4837 — 15 CommunitySection skill cards all carry `tagline:'agen lo'`; `lo` is the banned lo/gue register (CLAUDE.md) — replace_all `'agen lo'` → `'agen kamu'`.
- `[a11y]` assets/app.jsx:2842 — hero headline is `<h2>` and the page has NO `<h1>`; every section is `<h2>` so there's no top-level heading — promote the hero headline to `<h1>`.
- `[a11y]` assets/app.jsx:2887 + 5369 — the only `<main>` is the decorative mockup chat pane; real content has no `main` landmark — demote 2887 to `<div role="presentation">` and wrap content sections (5370-5385) in a real `<main>`.
- `[honesty]` assets/app.jsx:4942,4948 vs 4677-4846 — CommunitySection header "Skill yang sudah hidup di tim kamu / siap dipakai hari pertama" over 14:1 roadmap cards (only 1 `shipped:true`) rendered in completed past tense — soften header to cover roadmap (e.g. "Skill yang sedang kami siapkan") and make `shipped:false` bodies read illustrative/future.
- `[perf]` index.html:44 — framer-motion loads the UNMINIFIED dev build (~142 KB) on the render path while React/ReactDOM use `.production.min.js` — point to the minified UMD build or self-host tree-shaken.
- `[perf]` index.html:42-44 — React + ReactDOM + framer-motion (461 KB raw) all from unpkg (no SLA, extra DNS+TLS hop) gate hydration — self-host under `/assets/vendor/`.
- `[perf]` assets/app.jsx:1995,2046 — carousel mounts up to 18 `<video preload="metadata">` on first render (4th section, below fold), firing 18 concurrent metadata requests during first paint — render with `preload="none"`, flip to `metadata` inside the existing IntersectionObserver; don't render clone-copy videos.
- `[perf]` index.html:36 — Noto Serif SC (large CJK) loads in the blocking critical font request but is only used for hanzi far below the fold — drop it from the main `css2?` request and load scoped/`&text=`-subset.
- `[perf/jank]` assets/app.jsx:151-165 vs index.html:2018-2027 — #proses carousel runs ~130 simultaneous infinite CSS keyframe animations (10 cards × ~13) regardless of visibility, plus the hero DottedVideo getImageData loop runs on first paint — gate card scene animations on an IntersectionObserver class (animate only centered/visible cards) and defer the hero DottedVideo rAF start (~1s/requestIdleCallback).
- `[a11y/css]` index.html:1004-1021 + assets/app.jsx:5262 — `.hero-grain` (heroGrain 0.6s steps(6) infinite) in OriginSection has NO reduced-motion guard — add `@media (prefers-reduced-motion:reduce){.hero-grain{animation:none}}`.
- `[a11y/css]` index.html:528-531,2868 + assets/app.jsx:2839 — new hero `.db-eyebrow-pill .live-dot` + `.seats-foot::before` pulse `priceLiveDot ... infinite` with no RM guard (the circuit block right above IS guarded) — add `animation:none` for both in an RM block.
- `[a11y/css]` index.html:655-671,709-745,901-995 — new hero mockup `badgePulse`, `dbInputTyping 72s`, `pvBlink` caret, `typingPulse` dots, `bubbleMountIn`, `db-cursor` transitions all unguarded — neutralize inside one RM block (steady caret/dots, disable typer/cursor).
- `[a11y]` assets/app.jsx:5028-5041 + index.html:303-307 — FAQ trigger has `aria-expanded` but no `aria-controls`, and collapsed `.faq-body` (max-height:0/overflow:hidden) stays in the reading order so SR users hear all 7 answers regardless of state — add `aria-controls`/`id` + `role="region"`, and `hidden`/`inert` on closed panels.
- `[a11y]` assets/app.jsx:4423-4428 — ValueSlider readout updates with no `aria-live` and the input has no `aria-valuetext`, so SR announces a bare number with no unit — add `aria-valuetext={`${tasks} tugas per minggu`}` (or wrap `.vslider-readout` in `aria-live="polite"`).
- `[a11y]` index.html:2741-2749 + assets/app.jsx:2074-2085 — 10 carousel dot tabs are 6×6px (active 18×6), far below WCAG 2.5.8 24×24 touch target — give each `<button>` a `min-width:24px;min-height:24px` transparent hit area.
- `[a11y/contrast]` index.html:526 — `.db-eyebrow-pill` `#E5322D` on faint-red over dark ≈3.7:1 (10px uppercase) fails AA, over the busy hero; same red-on-dark for `.vslider-eyebrow` (100) and `is-chip--live` text (424) — lighten eyebrow red to ~`#FF6B66` (already used at 424).
- `[a11y/contrast]` index.html:329,427 — `.db-sub-micro` + `.is-tile-micro` `rgba(245,245,245,0.4)` ≈4.0:1 on ~10px (db-sub-micro over busy hero) fails AA — raise to `rgba(245,245,245,0.62)`.
- `[a11y/contrast]` assets/app.jsx:3890,4905-4906,4913 — strikethrough anchor price `text-white/35` (≈2.9:1) conveys real pricing info; community card meta white/40 — bump to at least `white/55`.
- `[conversion]` assets/app.jsx:2833,2856,5239 — hero proof (animated db-frame chat, 660/540px) sits below the fold behind an abstract dotted texture, and the concrete value "Empat app. Satu agen. Rp 99rb/bulan" is buried at the foot of section 2 — tighten mobile hero rhythm so the chat's top peeks in without scroll, and surface "Rp 99rb/bulan" higher (hero sub or IntegrationsSpine eyebrow).
- `[conversion/perf]` assets/app.jsx:476-483,5226-5228 — IntegrationsSpine iPhone slideshow is a 12s `isShotFade` loop (a 1s viewer sees one frozen shot) and has a black-flash gap at loop seams — speed to ~4-5s/cycle and overlap the fade-in (or set phone-screen bg to first shot to kill the gap).
- `[perf]` assets/app.jsx (below-fold sections) — no `content-visibility` anywhere; ~13 heavy sections fully laid out/painted up front — add `content-visibility:auto; contain-intrinsic-size:1px 800px;` to below-hero section classes (`.is-section`, `.origin-section`, `#proses`, `#pricing`).

## P2 — nice to have (polish)

- `[css]` index.html:54-65,118 + assets/app.jsx:3855 — featured pricing card runs 3 concurrent infinite box-shadow paints (priceGlowPulse + cta-pulse + ctaSheen) — move the glow to an `::after` opacity animation or drop one loop.
- `[css]` index.html:504 — `@keyframes isBar` has zero consumers (dead code) — delete.
- `[css]` index.html:407,420-496 — `.is-item--segera`/`.is-tile--segera*`/`.is-chip--segera`/`.is-phone-badge` are dead while all tiles are `live`; resolved automatically once School Expert flips to Segera (P0).
- `[css/perf]` index.html:1133-1145,3096-3162,1205-1217,2876-2886 — `pvMemFill`/`howLine`/`howmock .fill`/`ucScan`/`capBriefSun` animate layout/inset props (right/width/top/bottom) instead of transform — convert to `transform: scaleX/translate` where feasible.
- `[css]` index.html:344-2901 — pre-existing `how*`/`pv*`/`cap*`/`uc*` animation systems (incl. two infinite marquees + radar sweep) have no RM guard though still rendered — one consolidated RM block setting `animation:none` (at minimum pause the marquees + sweep).
- `[a11y]` assets/app.jsx:418 — logo img `alt` + link `aria-label` double-announce the brand — set img `alt=""`.
- `[a11y]` assets/app.jsx:5096 — CtaFooter is `<section>`, no `contentinfo` landmark — change to `<footer>`.
- `[a11y]` assets/app.jsx:2498-2506,3684-3698 — PriceBreakdownModal (the only rendered one) has Escape + initial focus but no focus trap — cycle Tab within the dialog, restore focus to trigger on close.
- `[a11y]` assets/app.jsx:2715-2925 — DashboardDemo JS chat (typewriter/cursor/auto-advance) ignores reduced-motion — read `matchMedia('(prefers-reduced-motion:reduce)')` and render a settled static thread.
- `[voice/BI]` assets/app.jsx:3236 — VelvetSection "Code Wizard akan ngambil instruksi kabur ... end to end" reads as broken BI + loose English — rewrite "Code Wizard akan ambil instruksi yang masih kabur, lalu selesaikan skripnya — dari awal sampai jadi."
- `[wordiness]` assets/app.jsx:2690,3045,5114 — StartSection sub (~28w), FeaturesChess sub (~30w), CtaFooter sub (~24w) exceed the one-idea/≤18-word budget — tighten each to a single line (drop the price/feature tail in CtaFooter).
- `[consistency]` assets/app.jsx:1826,1828 — Email/Calendar descs are present-tense ("Rapikan kotak masuk." / "Atur jadwal kamu.") though the "Segera hadir" chip (2047) carries tense — optional: prefix "Segera:".
- `[consistency/css]` index.html:397,519-527 + assets/app.jsx:2680,3577,4577 — three distinct eyebrow-pill treatments (`.is-eyebrow`, `.db-eyebrow-pill`, `.liquid-glass`) on one page; `.db-frame` radius 14px is the odd one out vs 16-18px elsewhere — standardize on the JetBrains-mono red-tint pill and bump `.db-frame` to 16px.
- `[css/palette]` index.html:586-642 — `#6FA08F` sage + `#d9cdb6` sand accents in the hero mockup deviate from the locked red-on-dark palette — recolor to red tints/neutral greys or document the exception.
- `[confirm-intent]` assets/app.jsx:4509-4528 + tier-personas.ts:162 — Library Lengkap shows no strikethrough though a 999k→799k display-only anchor is documented; PricingCard only strikes `if (tier.priceStrike)` and no tier sets it — add `priceStrike:'Rp 999rb'` if the anchor should display, else leave (flagged so the loop neither restores nor removes blindly).
- `[cleanup]` assets/app.jsx:1345-1691,3835-3857 — `AgentScene*`/`SceneShell`/`CreatureSprite`/`CREATURES`/`gridDots` and the `bare` decoy PricingCard branch are never rendered (dead code, incl. English leftovers like "Driven by Fed rate uncertainty" :1411) — delete.
- `[perf]` assets/app.jsx:5227 — 12 phone screenshots (816 KB PNG) are correctly lazy but carry no `width`/`height`; could be ~250 KB as WebP — add intrinsic dimensions and convert to WebP.

Note: the copy/voice dimension found the HARD gates clean — zero exclamation marks, zero banned words in rendered strings (the `just` hits are CSS/code comments only), demo-honesty strings intact (2723, 2732), and pricing literals/slugs match tier-personas.ts with no drift. The School Expert item (P0) is the one true honesty violation.

Fix order for the loop: ship P0 first in this order — School Expert→Segera (5178), the two CSS one-liners (hero-circuit z-index 333, generic .live-dot), kill the 1.32 MB preload (index.html:39), neutralize the 4 dead buttons, then the 2-CTA collapse and OriginSection demote/reorder — each is low-risk and independently shippable before working down P1 contrast/a11y/perf in file-clustered batches.
