# Handoff brief — weuseai.agent (2026-07-01)

You're picking up a solo founder's project (Richie). **First, read `CLAUDE.md` (root) and `MEMORY.md` + the memory files** — they auto-load and hold the locked business model, tech stack, and prior context. This brief is the *delta* from the last working session plus what to do next.

Work from `/Volumes/Extreme SSD/Projects/weuseai.agent/velorah`.

---

## 1. Where things stand right now

**Two open PRs — branch only. Do NOT merge or push to `main`; Richie merges. (He explicitly authorized this session's payment + landing/checkout work.)**

- **PR #276** — `fix/payment-cors-chain` → the payment fixes. **Already deployed to prod.**
- **PR #275** — `landing/redesign-konten-port` → the new landing + checkout design + the marketing doc.

### Payment was silently broken for ~3 weeks — now FIXED + deployed
- **Root cause:** the `subscriptions_tier_check` DB constraint only allowed the *legacy* slugs `('starter','pro','studio')`. Every v1.4 checkout (`library-full`/`solo`/`voice-starter`/`done-for-you`, live since 2026-06-09) was rejected on the pending-subscription insert → create-invoice threw → raw 500 with no CORS headers → the browser mislabeled it a "CORS error."
- **Fix (applied to prod):** migration `supabase/migrations/20260701120000_fix_subscriptions_tier_check_v14.sql` widens the constraint (ALTER already run via `supabase db query --linked`). Also: `_shared/cors.ts` now reflects the request origin from an allowlist (weuseai.id/www/*.vercel.app/localhost); create-invoice wrapped in try/catch (cors-wrapped JSON errors, never a raw 500); `PUBLIC_BASE_URL=https://www.weuseai.id`; retry-pending-provisions RPC param fix.
- **Verified:** all 4 plans return HTTP 200 + a Xendit **test-mode** invoice URL (amounts 501486 / 702886 / 904286 / 1407786, matching the checkout display).

### Landing + checkout (PR #275)
- `index.html` IS now the "Konten Hero Motion" **design file itself** — vendored DC runtime in `assets/vendor/` (no CDN/Babel), CTAs wired by `assets/konten-wiring.js`, hero terminal = the previous rich `DashboardDemo` (in `assets/app.jsx`, mounted via `assets/konten-hero-demo.js`).
- `checkout-new.html` = the new checkout design, plan-aware, wired to create-invoice (verified). **Not yet swapped** to `checkout.html`.
- Founder directive on the landing: **"use the file, keep everything"** — the design's fabricated marketing (363/500 fake scarcity + MM:SS countdown, "Rp 34jt GRATIS", "Akurat 99%") is deliberately KEPT for now (flagged in PR #275 + the marketing doc §10).

---

## 2. What to do next (priority order)

1. **Remind Richie — founder-only (needs the Supabase dashboard, the CLI can't):**
   - **Rotate Xendit to production** (`xnd_production_*` key + prod `XENDIT_WEBHOOK_TOKEN`, redeploy `xendit-webhook` + `create-invoice`). Last thing before real money — currently test mode.
   - **Recovery net** — in Supabase SQL editor: `CREATE EXTENSION IF NOT EXISTS pg_net WITH SCHEMA extensions;` then set the GUCs `ALTER DATABASE postgres SET app.retry_pending_provisions_url/token` (+ `fleet_sentinel_*`, `library_refine_*`) using the service_role key. (`supabase db query --linked` CAN run `ALTER TABLE` but NOT `ALTER DATABASE SET` — that needs the privileged `postgres` role, dashboard-only.)
2. **Finish the checkout swap:** add the `?error=failed` return banner to `checkout-new.html` (Xendit bounces failed payers back to `PUBLIC_BASE/checkout.html?plan=…&error=failed`), then swap it to `checkout.html` (mirror the index.html swap). Verify create-invoice still fires from the browser.
3. **Consistency fix:** the landing stepper (in `index.html`) says "Lima menit" — but the honest/measured figure is **~8 menit** (the FAQ, checkout copy, and Phase F ~6.7-min measurement all say ~8). Align it.
4. **Once Xendit is prod:** run ONE real test payment end-to-end and watch the chain (create-invoice → xendit-webhook → Fly `/spin-up` → setup-script → bundle-pull → Telegram pairing → proactive greeting) → confirm the agent turns on. Do this yourself where possible; watch within the 15-min flow window.
5. **Honesty items (Richie's call):** if he says "make honest," replace the kept fabrications with the real `/api/public/subscription-count` counter, drop "99%", keep only the honest 799→999-after-1000 line. See marketing doc §10.
6. **Small:** footer socials ship `href="#"` (need real URLs). The unwired create-invoice 799→999 flip at `paid_customers≥1000` (see CLAUDE.md Launch FOMO — required before the 1000th sale).

---

## 3. How to work here (the locks)

- **Branch only; never push `main`/deploy-prod without Richie's OK** — but you MAY deploy edge functions + run SQL: the `supabase` CLI is logged in, project **`gtjgsligllbjcisiyrah`** linked. Deploy: `supabase functions deploy <fn> --project-ref gtjgsligllbjcisiyrah`. SQL: `supabase db query --linked "..."`.
- **Gates:** `npx tsx --test tests/landing-build.spec.ts tests/landing-pricing-drift.spec.ts` (repo uses `tsx --test`, NOT jest). The legacy React landing (`assets/app.jsx` → `node scripts/build-landing.mjs`) is unused by the live page but the gates still read it — keep it green.
- **Local visual verify:** restart `python3 -m http.server 88xx` **from the repo root** first (a `curl -o /dev/null` check false-passes on a 404 — confirm real HTML with `curl -s …|head -c 20`). Playwright: `file:///opt/homebrew/lib/node_modules/playwright/index.mjs`.
- **Catalog (v1.4, LOCKED):** bare 99 / solo 399 / voice-starter 599 / library-full 799 (anchor ~~999~~, "Rekomendasi kami", 10 personas) / done-for-you 1.299jt (8 personas + web app) / enterprise. Hosting 99k/mo, Always-On +49k/mo. Fee: QRIS 0.7% (total = (setup+99k+addon)×1.007). **Checkout slugs are `solo`/`voice-starter`/`library-full`/`done-for-you` — NEVER `premium`/`ultimate`/`pro`.**
- **Brand voice:** Bahasa, address as `kamu` (never Anda/lo/gue), ZERO exclamation in body, banned words (basically/just/literally/honestly/kind of/pretty much/revolutionary/disrupt/10x/game-changer/next-level), calm-premium, #0a0a0a bg / #E5322D accent, Inter + Instrument Serif, max 1 emoji/section.
- **Honesty (hard):** the agent DRAFTS and the customer approves + sends — NEVER claim auto-send/auto-post, live Gmail/Calendar/WhatsApp/CRM access, fabricated scarcity, or unverifiable stats. Full boundary in `docs/marketing/2026-07-01-launch-enablement.md` §10.
- **Founder style (memory):** do deploys/verification yourself, think several steps ahead, minimize his manual steps. When he shares secrets in chat, use them (no lecture). Save durable non-obvious facts to memory (`~/.claude/projects/.../memory/`, index in MEMORY.md).

## 4. Key pointers
- `CLAUDE.md` (root) — the locked brief. `MEMORY.md` — the memory index.
- `docs/plans/2026-06-30-konten-migration-log.md` — the landing redesign log.
- `docs/marketing/2026-07-01-launch-enablement.md` — the marketing/brand doc (grounded + honesty guardrails).
- PRs: #275 (landing/checkout/marketing), #276 (payment fixes, deployed).
