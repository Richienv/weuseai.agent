# Overnight Migration Charter — Port "Konten Hero Motion" design into the production React-via-CDN landing without breaking any system connection

_Generated by the ultracode migration-prompt engine (w3ip3ppry), 2026-06-30. This is the canonical overnight execution charter for porting the new "Konten Hero Motion" design into the production landing._

## Objective
Replace the current production weuseai.agent landing (velorah/assets/app.jsx + velorah/index.html one inline <style> → node scripts/build-landing.mjs → assets/app.js + assets/tw.css, deployed static on Vercel) with the visual design of /Users/richiekidnovell/Downloads/motion-graphic-references-request/project/"Konten Hero Motion.dc.html", by REBUILDING each design section as React components inside the existing production pipeline — never shipping the Design-Component runtime (support.js), never hand-editing app.js/tw.css. The work runs on a dedicated branch, one section per iteration, each iteration gate-green (npm test) + Playwright-verified + wiring-checked + committed, so the live funnel (checkout, contact, scarcity counter, persona pages, legal pages) keeps working byte-for-byte. Every CTA in the design is a dead href="#"/<button>; every one must be wired to its REAL production target. The locked v1.4 catalog (tier-personas.ts) and the honesty locks win over any pricing/copy the design shows. DONE = the new design is live, all gates green, every CTA resolves to its documented target, and no honesty/brand/pricing lock is violated.

## Pricing reconciliation (catalog wins)
CATALOG WINS — supabase/functions/_shared/tier-personas.ts (TIERS) is the single source of truth, mirrored in app.jsx and pinned by landing-pricing-drift.spec.ts. Map the design's 5 visible tiers to catalog slugs by FEATURE, not by label:\n- Design 'Starter' (Rp 399rb, 3 persona, NO voice) ≡ catalog `solo` (399_000, 3 persona, no voice). CTA → checkout.html?plan=solo.\n- Design 'Pro' (Rp 599rb, 3 persona + voice) ≡ catalog `voice-starter` (599_000, 3 persona + voice). CTA → checkout.html?plan=voice-starter. (Do NOT emit ?plan=pro — that legacy alias resolves to done-for-you and would mis-sell.)\n- Design 'Premium / REKOMENDASI KAMI' (Rp 799rb, 10 persona, NO web app) ≡ catalog `library-full` (799_000, anchor strike 999_000, all 10 persona). This is the featured card with the red pulsing glow + the ONLY strikethrough on the page. CTA → checkout.html?plan=library-full.\n- Design 'Ultimate' (Rp 1,299jt, 8 persona, WITH web app) ≡ catalog `done-for-you` (1_299_000, 8 persona, web_app). CTA → checkout.html?plan=done-for-you. CRITICAL: ?plan=ultimate is not a slug and falls back to library-full (wrong tier, wrong charge) — emit the canonical done-for-you slug.\n- Design 'Enterprise' ≡ catalog `enterprise` (contact-only). CTA → mailto + wa.me, no self-serve checkout.\n- Hidden: catalog `bare` (99_000) stays in the app.jsx `tiers` array (decoy:true) for the drift gate even though no card is shown.\n\nNumbers locked by the gate (must appear verbatim in app.jsx): setupIdr 99_000 / 399_000 / 599_000 / 799_000 / 1_299_000; priceLabel 'Rp 99rb' / 'Rp 399rb'; month1Total 'Rp 198rb' / 'Rp 498rb' / 'Rp 698rb' / 'Rp 898rb' / 'Rp 1,398jt'; the 999_000 anchor only on library-full. Hosting Rp 99rb/bulan on every tier. The server (create-invoice) recomputes the charge from tier-personas.ts — display prices are advisory.\n\nSCARCITY DIVERGENCE (do NOT trust the design): the design's '500 orang pertama / Slot terisi 363 / 500 / harga naik permanen' static box and the earlier countdown-timer variant are ILLUSTRATIVE and must be discarded. The REAL mechanism (CLAUDE.md + MEMORY): a 1.000-customer launch batch read live from /api/public/subscription-count (paid_customers), TOTAL hardcoded 1000, banner renders nothing on fetch failure; and the honest forward-looking line 'Setelah 1.000 pertama, setup Library Lengkap naik dari Rp 799rb ke Rp 999rb'. Keep exactly the existing LimitedSeats() behavior.\n\nDESIGN-ONLY MARKETING FRAMING (keep as illustrative copy, NEVER wire to a price/checkout): the Explore-section struck 'Rp 27.500.000', the pricing value-banner struck 'Rp 34.000.000 → GRATIS', and the Apps-section per-app 'Harga normal Rp 1.49jt / Rp 990rb → GRATIS'. None are catalog SKUs.\n\nFLAG TO FOUNDER (do not silently resolve): (1) the design Premium=library-full mapping means the recommended card is 10-persona/no-web-app at 799 — confirm this is the intended hero tier (it matches the locked v1.4 matrix where library-full is Rekomendasi). (2) The unwired create-invoice 799→999 flip at paid_customers≥1000 is a REAL outstanding follow-up — the page advertises 999 but create-invoice still charges 799; before the 1000th sale this must be wired or the claim breaks. Surface it in the PR, do not attempt it inside this design migration.

## Hard limits
- NEVER ship support.js (the 55KB DC runtime) or any in-browser compiler. index.html must contain ZERO occurrences of 'babel', 'cdn.tailwindcss.com', 'hls.min.js', 'type="text/babel"' — landing-build.spec.ts greps index.html for these and fails the build.
- NEVER hand-edit assets/app.js or assets/tw.css. They are generated. Edit assets/app.jsx and the single <style> block in index.html, then run `node scripts/build-landing.mjs` from velorah/ and commit BOTH artifacts. The freshness gate rebuilds and asserts byte-for-byte equality.
- NEVER delete the Bare Agent tier object from the `tiers` array in app.jsx Pricing() (it is hidden via decoy:true/visibleTiers, but the drift gate greps slug:'bare', setupIdr:99_000, priceLabel 'Rp 99rb', month1Total 'Rp 198rb'). Hide cards, never delete catalog objects.
- NEVER fabricate the scarcity number. LimitedSeats() must keep reading GET /api/public/subscription-count (paid_customers) with the 3s AbortController; on any failure it returns null and renders NOTHING. The design's '500 orang pertama / 363 / 500 / harga naik permanen' static copy is ILLUSTRATIVE and must be replaced by the real counter behavior + the honest forward-looking '799→999 setelah 1.000 pertama' line. No fake countdown clock.
- Catalog wins over design pricing. Locked v1.4 setup fees: bare 99_000, solo 399_000, voice-starter 599_000, library-full 799_000 (anchor strike 999_000 — the ONLY strikethrough on the page), done-for-you 1_299_000. Hosting Rp 99rb/bulan every tier. month1Total literals: 198rb/498rb/698rb/898rb/1,398jt. The design's 'Premium Rp 799rb / 10 persona / no web app' = catalog library-full; design's 'Ultimate Rp 1,299jt / 8 persona / web app' = catalog done-for-you. Any new Rp amount in the design that is not in the catalog is marketing framing only and must NOT become a wired price.
- Preserve checkout slug contract: checkout.html?plan=<slug> with slugs {bare, solo, voice-starter, library-full, done-for-you}; LEGACY_PLAN_ALIAS {starter→voice-starter, pro→done-for-you, studio→library-full}; default/unknown → library-full. Do NOT rename design CTAs to slugs that don't resolve (e.g. ?plan=premium would silently fall back to library-full — acceptable only if intended; ?plan=ultimate would fall back to library-full and MIS-SELL — must map to done-for-you).
- Preserve the CTA target/rel rule (app.jsx PricingCard ~4015-4018): external http(s)/mailto CTAs get target=_blank, http(s) get rel=noopener, internal (checkout.html?plan=) stay same-tab.
- Brand locks: Bahasa Indonesia, address reader as 'kamu' (never 'Anda'/'lo'/'gue'). ZERO exclamation marks in body copy (max 1 per caption). BANNED words anywhere: basically, just, literally, honestly, kind of, pretty much, revolutionary, disrupt, 10x, game-changer, next-level. Max 1 emoji per section.
- Demo-honesty gate: app.jsx must NOT contain any of the banned fabricated-integration strings (Sorted, emails, GST, 'PR #142', Auto-publish, 'Calendar update', 'confirmed dalam', 'Live di 6 platform', overnight, 'trending apa', Auto-monitor, 'otomatis ke OLX', 'kalender di-sync', 'Otomatis dibaca', '10×') and MUST contain 'Pagi Briefing — dikirim otomatis' + 'Aku tidak mengirim apa pun tanpa kamu setujui'. Audit every ported demo bubble against this before commit.
- No new npm packages, no new vendored libraries beyond the existing /assets/vendor (react, react-dom, framer-motion). Ambient motion must be CSS keyframes (add to the single index.html <style> block) or framer-motion (Mot = M.motion) — never the DC runtime. JS-only-where-needed (stage scaling, slider math, count-ups, auto-scroll, node-sequence) must be wired in React useEffect, NOT a componentDidMount/renderVals pattern.
- Do NOT ship any image from /uploads or /screenshots — they are dev history. The canonical design HTML has zero <img>/url() raster refs; reproduce every visual as inline SVG + CSS gradients (matching the existing app.jsx approach). Reuse existing /assets/*.mp4 and /assets/r2-*.png only where the production app already does.
- Work on a branch off the current branch (landing/enhance-conversion) — never commit the migration directly to main. main auto-deploys to Vercel; an unverified push goes live. Run git from inside velorah/ (the repo root is velorah/, not the SSD project folder).

## Wiring checklist — every CTA → real target (the "nothing breaks" contract)

- **Nav 'Mulai ↗' (currently <button>, dead)** → <a href="#pricing"> same-tab (matches production Navbar app.jsx 441/459) _In-page scroll anchor to the Pricing section id='pricing'._
- **Nav links 'Kerja' / 'Harga' / 'FAQ' (dead <a> no href)** → #proses / #pricing / #faq respectively (production Navbar app.jsx 423-426 uses Beranda→#beranda, Kerja→#proses, Harga→#pricing, FAQ→#faq) _Keep the production label set including 'Beranda'→#beranda if the section ids are preserved; the section ids the new components render MUST include beranda/proses/pricing/faq or the anchors break. Search input is decorative — keep non-functional or drop it; do not wire a fake search._
- **Nav logo (weuseai.agent diamond + wordmark)** → href="/" (production app.jsx 432)
- **Hero primary 'Aktifkan asisten kamu ↗' (dead <button>)** → <a href="#pricing"> same-tab (matches production hero CTA app.jsx 2888/5186/5390) _White-pill/3D-red styling is fine; the destination is the pricing section._
- **Hero ghost 'Konsultasi gratis (15 menit)' (dead <button>)** → https://cal.com/weuseai.agent/15min target=_blank rel=noopener (production demo CTA app.jsx 2891/5189/5394) _This is the real demo-booking link; preserve target/rel._
- **Pricing 'Mulai Starter ↗' (Starter tier)** → checkout.html?plan=solo OR checkout.html?plan=voice-starter — RECONCILE: design 'Starter' (Rp 399rb, 3 persona, no voice) = catalog 'solo'; design 'Pro' (Rp 599rb, voice) = catalog 'voice-starter' _Map by FEATURE not by label. See pricingReconciliation. Starter(no voice)→solo; Pro(voice)→voice-starter._
- **Pricing 'Naik ke Pro ↗' (Pro tier)** → checkout.html?plan=voice-starter (Rp 599rb, 3 persona + voice) _'pro' as a raw ?plan= would alias to done-for-you — do NOT use ?plan=pro; emit the canonical ?plan=voice-starter._
- **Pricing 'Ambil Premium ↗' (Premium = REKOMENDASI KAMI)** → checkout.html?plan=library-full (Rp 799rb setup, anchor strike Rp 999rb, all 10 persona). This is the featured/recommended card — keep the red pulsing glow (priceGlowPulse). _library-full is the ONLY card that shows a strikethrough (999 anchor). Keep the 'Rekomendasi kami' badge + glow._
- **Pricing 'Ambil Ultimate ↗' (Ultimate tier)** → checkout.html?plan=done-for-you (Rp 1,299jt, 8 persona + web app) _CRITICAL mis-sell risk: ?plan=ultimate is NOT a valid slug and would fall back to library-full, charging the wrong tier. Emit ?plan=done-for-you._
- **Pricing 'Hubungi Sales ↗' (Enterprise)** → Primary: mailto:kidnovell.richie@gmail.com?subject=Enterprise%20weuseai.agent (target=_blank). Secondary WhatsApp: https://wa.me/6282154902561?text=Halo%2C%20saya%20tertarik%20paket%20Enterprise (target=_blank rel=noreferrer). Both per production app.jsx 4688/4694. _Enterprise is contact-only, NO self-serve checkout._
- **Per-tier secondary 'Lihat rincian biaya →' (dead)** → onClick → open the in-page PriceBreakdownModal for that tier (production app.jsx 4029-4036; modal footer 'Lanjut ke checkout →' → checkout.html?plan=library-full at app.jsx 5474) _Reuse the existing PriceBreakdownModal component; it computes from setupIdr + HOSTING_MONTHLY. Do not invent a new breakdown._
- **Skills-grid footer micro-CTA 'Lihat cara orang pakai'** → use-cases.html (production use-cases footer link app.jsx 3542 uses the .html extension, not /use-cases) _Use the .html form to match production exactly._
- **Skills-grid card 08 'Coding' SEGERA** → If it carries a CTA: https://wa.me/6282154902561?text=Halo%20Richie%2C%20mau%20early%20access%20Code%20Wizard%20agent%20di%20weuseai.agent target=_blank (production Coding roadmap ctaHref app.jsx 3279/3533). Otherwise leave as a non-interactive SEGERA chip. _Match the early-access WhatsApp link if a CTA is rendered._
- **FAQ 'Punya Pertanyaan? [WhatsApp]' (dead href=#)** → https://wa.me/6282154902561 (support/contact). Production FAQ also exposes 'Lihat semua pertanyaan' → /faq (app.jsx 5109). _Wire to WhatsApp; preserve the /faq 'see all' link. FAQ accordion: reimplement in production React useState (openFaq), not DC-runtime state._
- **Footer nav 'Beranda / Cara Kerja / Harga'** → #beranda / #proses / #pricing (in-page anchors)
- **Footer 'Privacy Policy' / 'Terms of Service'** → /privacy and /terms (cleanUrls; production footer app.jsx 5202-5208). Also keep /refund-policy + /contact wired if rendered. _vercel.json cleanUrls:true, trailingSlash:false. The /privasi→/privacy, /syarat→/terms, /kontak→/contact 308 redirects must keep resolving — do not break them._
- **Footer socials Instagram / Discord / LinkedIn / TikTok** → Real URLs only if the founder confirms handles; otherwise OMIT the social column rather than ship href="#" dead links. Discord 'Gabung Discord ↗' pill → real invite URL or omit. _FLAG TO FOUNDER: production app.jsx footer does not currently expose these socials. Do NOT invent social URLs. Default to omit-until-confirmed._
- **Footer contacts halo@weuseai.id / sales@weuseai.id / +62 811-1309-8585** → Production uses mailto:support@weuseai.agent (app.jsx 5212). FLAG: the design emails (halo@/sales@weuseai.id) + phone differ from production. Use production's support@weuseai.agent + the enterprise mailto kidnovell.richie@gmail.com unless founder confirms the new addresses are live. _Do not ship a tel:/mailto: to an unverified address. Reconcile with founder._
- **Brand wordmark discrepancy: nav says 'weuseai.agent', footer says 'weuseai.id'** → Keep nav '.agent' and footer '.id' exactly as the design intends IF that matches current production usage (index.html canonical is https://weuseai.id/, OG site_name 'weuseai.agent'). Verify against production before changing either. _Both forms exist in production metadata; preserve the design's intentional split unless it contradicts a live page._
- **Persona/agent deep-links (if the ported persona/agent section renders cards)** → /persona?slug=<slug> (rewrite /persona/:slug→/persona.html); persona modal CTA → /checkout?persona=<slug>; agent caption 'harga →' → #pricing (production app.jsx 2079/2654/2695/2131) _Only applies if the migration keeps the existing AgentCarousel/PersonaDetailGrid. If the new design has no persona-detail section, these components can be dropped — but then remove their inbound anchors too._

## Build order (one verified section per iteration)

0. **Branch + baseline snapshot + harness** — From velorah/: create branch landing/redesign-konten-port off landing/enhance-conversion. Run `npm test` and record the green baseline. Run `node scripts/build-landing.mjs` and confirm zero diff (clean tree). Capture Playwright baseline screenshots of the CURRENT live sections (hero, pricing, faq, footer) at 1440px + 390px for before/after comparison. Read the full design HTML once end-to-end. Commit nothing yet (snapshot only).
1. **Design tokens + fonts + keyframes foundation** — Add Plus Jakarta Sans to the production font <link> in index.html (extend the existing Google Fonts css2 link to include Plus+Jakarta+Sans:wght@400;500;600;700;800 alongside the existing JetBrains Mono; keep Instrument Serif+Inter only if still referenced elsewhere, else prune). Add the design's ambient @keyframes (travelFade, currentFlow, glowPulse, hubFloat/floatY, portPulse, dotPulse, caretBlink, dotBounce, barFill, drawLine, checkSeq, shimmer, slashIn, priceGlint, cardRise, heroReveal, lineGrow, numActive, stepGlow, chipFloat, radarSweep, etc.) into the SINGLE index.html <style> block, plus the prefers-reduced-motion reset. Add the color tokens as CSS custom properties or shared JSX constants. Do NOT change any component yet.
2. **Nav (Section 0) — sticky glass nav** — Port the design's fixed glassmorphism nav as the production Navbar replacement (keep component name Navbar, keep mobile hamburger behavior). Wire all CTAs per wiringChecklist (Mulai→#pricing, Kerja→#proses, Harga→#pricing, FAQ→#faq, logo→/). Keep section-id anchors intact.
3. **Hero (Section 1) — converging-channels terminal** — Replace DashboardDemo/hero with the converging-circuit hero (headline 'Satu agent. / Ngerjain kerja kamu.', subhead, terminal chat mockup, circuit rails + channel badges via offset-path, primary+ghost CTAs). Keep section id='beranda'. Reproduce all visuals as inline SVG+CSS; stage scaling in useEffect. Audit demo bubbles against the demo-honesty banned list; keep them within real capability.
4. **Explore Usage + Calculator (Section 2)** — Port the value headline, 3D '100+ TUGAS' token, struck Rp 27.500.000 value panel (marketing framing — NOT a catalog price, keep as illustrative copy with the slashIn animation), the interactive savings slider (count-on-drag in useEffect), and the 6 use-case cards with auto-scrolling chat panels. Audit EVERY chat snippet against demo-honesty banned strings; rewrite any that imply fabricated integrations.
5. **Memory & Persona proof (Section 3)** — Port the 99% count-up memory card + real-time learning panel + 3-node flow. Count-ups and node-sequence lighting in useEffect (IntersectionObserver to start). Keep copy honest (memory/persona claims are within capability per CLAUDE.md).
6. **Product Apps (Section 4)** — Port the 'Dia kerja di app beneran' app cards (R2 Content/R2 Fit + AKTIF/SEGERA pills + phone mockups). The 'Harga normal Rp 1.49jt → GRATIS' strikes are MARKETING framing, not catalog SKUs — keep as illustrative, do not wire to checkout. Reuse existing /assets/r2-*.png only if the production app already does; otherwise inline SVG mockups.
7. **Skills Grid (Section 5)** — Port the 8 capability cards (7 active + Coding SEGERA) with per-card micro-animations. Wire footer 'Lihat cara orang pakai' → use-cases.html; Coding early-access CTA → WhatsApp link if rendered. Audit micro-copy for honesty.
8. **How-It-Works Stepper (Section 6)** — Port the 4-step auto-advancing stepper on red dot-matrix bg. Step-1 mock shows 'PRO · DEDICATED Rp 99rb/bln' — this is a VISUAL only; ensure the Rp 99rb hosting figure matches the catalog (it does). numActive/stepGlow cycling via CSS or useEffect interval.
9. **Pricing (Section 7) — THE load-bearing section** — Rebuild the pricing visual on top of the EXISTING Pricing()/PricingCard()/LimitedSeats()/PriceBreakdownModal() logic — do NOT rewrite the catalog. Keep the full `tiers` array (incl. hidden Bare decoy). Map design tiers→catalog by feature (see pricingReconciliation). Wire every CTA to checkout.html?plan=<canonical-slug>. Replace the design's static '363/500 / harga naik permanen' scarcity with the REAL LimitedSeats counter + honest '799→999 setelah 1.000 pertama' line. library-full = featured (glow + 999 strike, the ONLY strike). Keep 'Lihat rincian biaya' → PriceBreakdownModal.
10. **FAQ (Section 8)** — Port the 6-question accordion + 'Punya Pertanyaan?' WhatsApp CTA. Reimplement accordion with production React useState (openFaq). Keep 'Lihat semua pertanyaan' → /faq. Reconcile the 6 design Q/As with the head FAQPage JSON-LD (index.html 59-122) — if the visible questions change, update the JSON-LD to mirror, keeping it rating-free/honest.
11. **Footer (Section 9)** — Port the footer (brand line, nav/contact columns, legal). Wire nav anchors + /privacy + /terms + /refund-policy + /contact. For socials + the design's halo@/sales@/phone: omit-until-founder-confirmed (do NOT ship href=# or unverified addresses); keep production support@weuseai.agent + enterprise mailto. Discord pill → real invite or omit.
12. **Cleanup + dead-code + final composition** — Remove any now-orphaned old components from app.jsx (only after confirming nothing references them). Update App() composition to the new section order, ensuring section ids beranda/proses/pricing/faq survive for the anchors. Prune unused @keyframes/classes so tw.css stays lean. Final `node scripts/build-landing.mjs` + commit both artifacts.
13. **Founder gate (do NOT merge autonomously)** — Open a PR from the branch with before/after screenshots, the wiring-checklist results table, the pricing-reconciliation note, and the list of founder-flagged items (socials, halo@/sales@ emails, phone, brand wordmark split, the unwired 799→999 create-invoice follow-up). Leave merge to main for founder review — main auto-deploys.

## Per-section spec

### 0 — Sticky glass nav
{
  "build": "Fixed top bar, 64px, z-50, glassmorphism (bg rgba(7,6,8,.55) + backdrop blur 18px saturate 160%, 1px bottom hairline). Diamond logo glyph + 'weuseai' white + '.agent' gradient. Right: text links + white-pill 'Mulai'. Keep production mobile hamburger.",
  "copy": "Links: Beranda / Kerja / Harga / FAQ. Button: 'Mulai ↗'. (Drop the decorative search input or keep it visually inert — do not wire a fake search.)",
  "motion": "Static bar, logo gloss. No scroll-state change required.",
  "wiring": "Mulai→#pricing; Beranda→#beranda, Kerja→#proses, Harga→#pricing, FAQ→#faq; logo→/."
}

### 1 — Hero converging channels
{
  "build": "id='beranda'. Headline block (62px, line1 gradient / line2 white), subhead. Center mac-window terminal chat (traffic lights, header 'weuseai.agent · dashboard' + Online, mono bubbles, file chip, typing dots, input caret). L-elbow circuit rails (base #3a2c28 + glow #ff6a4c currentFlow) into pulsing ports; channel badges riding offset-path. Inline SVG+CSS only. Stage scaling in useEffect on resize.",
  "copy": "Eyebrow 'Agen kamu bekerja'. Headline 'Satu agent. / Ngerjain kerja kamu.'. Subhead 'Semua yang bisa kamu kerjakan dari laptop, dia kerjakan untuk kamu — otomatis.'. Terminal bubbles per design (audit each vs demo-honesty list).",
  "motion": "heroReveal, currentFlow, travelFade/scanFade, portPulse, hubFloat, caretBlink, dotBounce, glowPulse, msgIn.",
  "wiring": "Primary 'Aktifkan asisten kamu ↗'→#pricing (same-tab). Ghost 'Konsultasi gratis (15 menit)'→https://cal.com/weuseai.agent/15min target=_blank rel=noopener."
}

### 2 — Explore Usage + calculator
{
  "build": "Two-col: left serif headline + body + 3D '100+ TUGAS' token + value panel (struck Rp 27.500.000 + GRATIS badge — illustrative). Right: interactive savings slider (10–1.000 tugas, live 'jam hemat' readout, gradient track fill via JS). Below: 6 use-case cards with auto-scrolling chat panels, hours stat, mono tag, 'Dikerjakan agent ##%' bar.",
  "copy": "Eyebrow 'Satu sistem, semua surface'. Title 'Tiap tool yang biasanya dijual mahal terpisah, jadi satu.'. Body per design. The Rp 27.5jt total + per-card hours are illustrative framing — keep, do not present as a catalog price.",
  "motion": "slashIn (strike), priceGlint, barFill/shimmer, drawLine, auto-scroll loop (useEffect), cardRise.",
  "wiring": "None (informational). The struck Rp values are NOT wired to checkout."
}

### 3 — Memory & Persona proof
{
  "build": "Title + body. Left stat card with gradient 0→99% count-up + shimmer accuracy bar + mono value rows. Right panel: 3 colored stat tiles (#128 blue, 1× amber, 247× green) + 3-node horizontal flow (numbered circles light sequentially).",
  "copy": "Title 'Ajarin sekali. Akurat 99% seterusnya.'. Body + tile/flow copy per design. Honest (memory/persona is real capability).",
  "motion": "count-up (useEffect + IntersectionObserver), node-sequence lighting, shimmer, drawLine, cardRise.",
  "wiring": "None."
}

### 4 — Product Apps
{
  "build": "Centered header + 'Satu agen' glossy pill. 4-col grid of app cards: icon disc, '0X · APP' kicker, serif name, AKTIF/SEGERA pill, price-strike→GRATIS block (illustrative), floating phone mockup with one signature loop.",
  "copy": "Title 'Dia kerja di app beneran.'. App names R2 Content/R2 Fit + 2 more. Price strikes (Rp 1.49jt etc.) are marketing framing, NOT catalog SKUs.",
  "motion": "chipFloat, floatY/colFloat, dotPulse (AKTIF), rowScan/checkSeq/barFill/drawLine in phones, cardRise.",
  "wiring": "None (cards non-interactive; price strikes not wired)."
}

### 5 — Skills grid
{
  "build": "Centered header. 4×2 numbered capability cards, each a small looping red-on-dark micro-animation + 'NN · SKILL' kicker + serif name + muted desc. Card 08 'Coding' as SEGERA.",
  "copy": "Title 'Skill siap pakai. Fresh out of the box.'. 8 skills per design. Audit micro-copy for honesty.",
  "motion": "per-card loops (msgIn, deckMove, radarSweep, typeLine, cellWrite, fileSnap, testGreen, caretBlink), cardRise.",
  "wiring": "Footer 'Lihat cara orang pakai'→use-cases.html. Coding SEGERA early-access (if CTA)→wa.me/6282154902561 early-access link, target=_blank."
}

### 6 — How-it-works stepper
{
  "build": "Red dot-matrix bg. Centered title. Auto-advancing numbered stepper (4 circles + lineGrow connector) over 4 animated step cards.",
  "copy": "Title 'Empat langkah. Delapan menit.'. Steps per design. Step-1 mock 'PRO · DEDICATED Rp 99rb/bln' — visual only; Rp 99rb matches catalog hosting (keep).",
  "motion": "lineGrow, numActive (i*2.5s, 10s loop), stepGlow, checkSeq, msgIn, caretBlink.",
  "wiring": "None (step-1 plan mock is a visual, not a button)."
}

### 7 — Pricing (LOCKED)
{
  "build": "id='pricing'. Reuse existing Pricing()/PricingCard()/LimitedSeats()/PriceBreakdownModal() logic; restyle to the design. Centered title + body + value banner (struck Rp 34.000.000 illustrative). REAL scarcity counter (not static 363/500). 5 visible cards: Starter/Pro/Premium(featured glow)/Ultimate + Enterprise band. Keep hidden Bare decoy in the array.",
  "copy": "Title 'Pilih ukuran tim kamu. Upgrade kapan saja.'. Body 'Bayar setup sekali, hosting transparan Rp 99rb/bulan... pause kapan saja.'. Scarcity: real counter caption + honest '799→999 setelah 1.000 pertama'. NO fake countdown. Keep the risk-reversal strip (VPS pribadi / jaminan 14 hari / berhenti kapan saja / setup tetap milik kamu) verbatim.",
  "motion": "priceGlint/sheenSweep, slashIn (999 anchor only), shimmer on real seats bar, priceGlowPulse on Premium/library-full, cardRise.",
  "wiring": "Starter→?plan=solo; Pro→?plan=voice-starter; Premium→?plan=library-full; Ultimate→?plan=done-for-you; Enterprise→mailto + wa.me; 'Lihat rincian biaya'→PriceBreakdownModal; modal footer→checkout.html?plan=library-full."
}

### 8 — FAQ
{
  "build": "id='faq'. Two-col: left sticky title + WhatsApp CTA; right 6-row accordion (React useState openFaq). Reconcile visible questions with head FAQPage JSON-LD.",
  "copy": "Title 'Frequently Asked Questions'. 6 Q/As per design (audit honesty; the 'pre-fine-tune', 'VPS dedicated', '8 menit', 'approve sekali tap' claims align with CLAUDE.md).",
  "motion": "accordion max-height/opacity, eyebrow dot, cardRise.",
  "wiring": "'Punya Pertanyaan?'→wa.me/6282154902561. 'Lihat semua pertanyaan'→/faq."
}

### 9 — Footer
{
  "build": "Dark footer band: brand blurb + Navigasi/Social/Kontak columns + Discord pill + legal line.",
  "copy": "Brand 'weuseai.id' + 'Stand taller.' + blurb. Legal '© 2026 PT WEUSEAI TEKNOLOGI INDONESIA...'. Reconcile contacts with production.",
  "motion": "static; Discord pill hover translateY(-2px).",
  "wiring": "Nav→#beranda/#proses/#pricing; Privacy→/privacy; Terms→/terms; (+/refund-policy,/contact if rendered); support mail→mailto:support@weuseai.agent; socials + halo@/sales@/phone + Discord = omit-until-founder-confirmed (NO href=#)."
}

## Verification
PER-ITERATION GATE PROTOCOL (run all, in order, before each commit):\n1) BUILD: from velorah/, `node scripts/build-landing.mjs` — must succeed; commit BOTH assets/app.js + assets/tw.css alongside the app.jsx/index.html change.\n2) GATE: `npm test` (tsx --test tests/*.spec.ts) — must be fully green. The four load-bearing landing specs MUST pass every iteration: landing-build.spec.ts (freshness byte-for-byte + head-purity no-babel/no-cdn-tailwind/no-hls/no-text-babel + bundle sanity >50KB/createRoot/no-import + demo-honesty banned+required strings), landing-pricing-drift.spec.ts (setupIdr literals 99_000/399_000/599_000/799_000/1_299_000 + slugs + 999.000 anchor round-trip + no 249_000/Rp 348rb), subscription-count-endpoint.spec.ts, plans-v14-pricing.spec.ts. A red gate blocks the commit — fix forward, never weaken a test.\n3) PLAYWRIGHT (mcp__playwright or local): serve the static site locally (e.g. npx serve velorah or a static server honoring vercel.json cleanUrls), navigate to the home page. (a) assert ZERO console errors; (b) screenshot the just-built section at 1440px, 768px, 390px and visually compare to /Users/richiekidnovell/Downloads/motion-graphic-references-request/project/screenshots/ + the section's design reference; (c) toggle prefers-reduced-motion and confirm animations stop (the * {animation:none} rule).\n4) WIRING-CHECK (DOM assertions, the 'nothing breaks' contract): for every CTA in this section, query the rendered anchor/button and assert its href/target/rel EXACTLY equals the wiringChecklist target. For pricing, assert each card's CTA href is the correct checkout.html?plan=<slug> (especially Ultimate→done-for-you, never falling back to library-full). For the scarcity banner, intercept /api/public/subscription-count: return a valid {paid_customers:N} and assert the count renders; return a 500/timeout and assert the banner renders NOTHING (null). For internal nav, assert clicking an anchor scrolls to the live section id.\n5) COMMIT with a [landing] message describing the ported section.\n\nFINAL VERIFICATION (iteration 12, before the founder PR):\n- Full `npm test` green from a clean tree; build reproduces artifacts byte-for-byte (no uncommitted diff after rebuild).\n- Full-page Playwright pass at 1440/768/390 + reduced-motion, zero console errors.\n- WIRING MATRIX: walk the entire wiringChecklist; every CTA/endpoint/page resolves to its documented target. Confirm checkout.html?plan=<each slug> loads and resolves the right tier; confirm /faq, use-cases.html, /privacy, /terms, /refund-policy, /contact, /persona?slug=... (if rendered) all 200. Confirm cal.com + wa.me + mailto links carry correct target/rel.\n- HONESTY AUDIT: grep app.jsx for the demo-honesty banned list (0 hits) + the 2 required strings (present); confirm the only strikethrough on the page is the library-full 999 anchor; confirm no fake countdown clock; confirm scarcity reads the real endpoint.\n- BRAND AUDIT: grep for banned words (basically/just/literally/honestly/kind of/pretty much/revolutionary/disrupt/10x/game-changer/next-level) and for '!' in body copy; confirm 'kamu' not 'Anda'.\n- Open PR (do not merge); attach before/after screenshots + wiring matrix + reconciliation note + founder-flag list.

## Risks
- MIS-SELL via slug fallback: emitting design labels as ?plan= (premium/ultimate/starter/pro) silently resolves to the wrong tier (unknown→library-full; pro→done-for-you; studio→library-full). Mitigation: hard-assert each pricing CTA href against the canonical slug in the wiring-check; Ultimate→done-for-you is the highest-stakes one (1.299jt vs 799rb).
- Freshness-gate trap: forgetting to rebuild after an app.jsx/index.html change ships a stale page AND fails landing-build.spec.ts. Mitigation: every iteration ends with `node scripts/build-landing.mjs` + commit both artifacts; the gate rebuilds and diffs.
- Head-purity regression: re-introducing the DC runtime (support.js), Babel, or cdn.tailwindcss.com into index.html. Mitigation: never copy the design's <script src=./support.js> or <helmet> runtime hooks; the gate greps index.html for these substrings.
- Honesty drift: porting design demo bubbles verbatim re-introduces banned fabricated-integration strings, or the static 363/500 scarcity replaces the real counter. Mitigation: demo-honesty gate + manual audit each ported chat snippet + keep LimitedSeats() endpoint behavior untouched.
- DC-runtime lifecycle mismatch: the design starts JS loops from renderVals behind a _started guard (DC hot-reload quirk). Naively porting that to React breaks. Mitigation: rewrite all JS (stage scaling, slider, count-ups, auto-scroll, node-sequence) as React useEffect with IntersectionObserver; prefer CSS reveals.
- Anchor breakage: the nav/footer in-page anchors (#beranda/#proses/#pricing/#faq) depend on the new components rendering those exact section ids. Mitigation: preserve the ids in every ported section; the wiring-check clicks each anchor.
- Font/China-robustness: production loads fonts from Google Fonts CDN (not self-hosted). Adding Plus Jakarta Sans via the same CDN link matches the current pattern but is NOT a robustness improvement; self-hosting woff2 is the safer path (vercel.json already caches /assets/*.woff2). Risk of over-reach: do not silently switch the whole font strategy mid-migration. Mitigation: extend the existing Google Fonts link in iteration 1; flag self-hosting as a follow-up unless the founder wants it now.
- Scope creep / accidental main push: main auto-deploys to Vercel. An autonomous loop committing to the wrong branch goes live. Mitigation: verify `git branch` shows landing/redesign-konten-port before every commit; never push to main; final step is a PR for founder review.
- Unverified footer data: socials/halo@/sales@/phone in the design may not be live. Shipping dead href=# or wrong mailto/tel harms trust. Mitigation: omit-until-confirmed; spawn a founder-flag task.
- Persona/agent section ambiguity: the new design has no dedicated persona-detail section, but production wires /persona?slug= and /checkout?persona= deep-links. Dropping those components silently breaks inbound persona hash links. Mitigation: decide explicitly — either keep a persona section or remove the inbound anchors too; do not leave dangling links.

---

## THE PROMPT (self-contained overnight charter)

／LOOP CHARTER — Migrate the new \"Konten Hero Motion\" landing design into the production weuseai.agent stack, section by section, gate-green and fully wired, without breaking the live funnel.\n\n=== ROLE ===\nYou are the migration engineer for weuseai.agent. The founder built a new flashy landing design and wants it to REPLACE the current production landing. Your job: PORT the design's sections into the existing precompiled React-via-CDN pipeline, wire every CTA to its real system target, keep every gate green, and never violate a pricing/honesty/brand lock. You run autonomously, one section per iteration, committing only verified work to a branch. You do NOT merge to main (main auto-deploys; the founder reviews the PR).\n\n=== GROUND TRUTH FILES (read before acting; repo root is velorah/, NOT the SSD folder) ===\n- Production source: /Volumes/Extreme SSD/Projects/weuseai.agent/velorah/assets/app.jsx (5480 lines; React via window.React, Mot = M.motion from framer-motion, classic JSX → React.createElement, Tailwind classes). App() composition at line ~5435 is the ordered section list. Pricing()/PricingCard()/LimitedSeats()/PriceBreakdownModal() at ~4468/3893/3674/3730. Navbar() at ~421. FAQ() at ~5041. CtaFooter() at ~5165.\n- The single hand-CSS block: /Volumes/Extreme SSD/Projects/weuseai.agent/velorah/index.html <style> (starts ~line 145) — the ONLY place to add @keyframes/CSS. Head fonts at line 126. Mount: <div id=\"app\"> + <script src=\"/assets/app.js\" defer> at ~4445/4447.\n- Build script: /Volumes/Extreme SSD/Projects/weuseai.agent/velorah/scripts/build-landing.mjs (esbuild app.jsx→app.js, tailwind→tw.css; tailwind pinned 3.4.17; content globs index.html+app.jsx+persona-details.js).\n- Gates: /Volumes/Extreme SSD/Projects/weuseai.agent/velorah/tests/landing-build.spec.ts, landing-pricing-drift.spec.ts, subscription-count-endpoint.spec.ts, plans-v14-pricing.spec.ts. Catalog source of truth: supabase/functions/_shared/tier-personas.ts (TIERS).\n- Checkout: /Volumes/Extreme SSD/Projects/weuseai.agent/velorah/checkout.html (PLANS map ~741, LEGACY_PLAN_ALIAS ~781, default library-full). Endpoint: api/public/subscription-count.ts. Routing: vercel.json (cleanUrls:true, trailingSlash:false; /persona/:slug rewrite; /privasi→/privacy etc. 308s).\n- THE NEW DESIGN (visual target only): /Users/richiekidnovell/Downloads/motion-graphic-references-request/project/\"Konten Hero Motion.dc.html\" (277KB DC export; data-screen-label sections: hero(none), Explore Usage, Memory dan Persona, Kerja di App, Skill Bawaan, Cara Kerjanya, Harga, FAQ + nav + footer). Spec: weuseai-design-system.md. Reference renders: screenshots/. Its CTAs are ALL dead (14 href=\"#\", 12 <button>). Its runtime is ./support.js — NEVER ship it.\n- Brand/business locks: velorah/CLAUDE.md (Brand voice rules; Business model v1.4 LOCKED; honesty locks). Memory: landing-redesign + persona-retier-is-a-trap + go-live-readiness notes.\n\n=== ABSOLUTE RULES (a single violation fails the run) ===\n1. NEVER ship support.js or any in-browser compiler. index.html must contain ZERO of: 'babel', 'cdn.tailwindcss.com', 'hls.min.js', 'type=\"text/babel\"'.\n2. NEVER hand-edit assets/app.js or assets/tw.css. Edit app.jsx + the one index.html <style> block, then `node scripts/build-landing.mjs` from velorah/, then commit BOTH artifacts.\n3. NEVER delete the Bare tier object from the app.jsx `tiers` array (decoy:true; the drift gate greps slug:'bare', setupIdr:99_000, priceLabel 'Rp 99rb', month1Total 'Rp 198rb'). Hide cards, never delete catalog objects.\n4. NEVER fabricate the scarcity number. Keep LimitedSeats() reading /api/public/subscription-count (paid_customers) with the 3s AbortController; render NOTHING on failure. Discard the design's static 363/500 box and any countdown. Keep the honest 'Setelah 1.000 pertama, setup Library Lengkap naik dari Rp 799rb ke Rp 999rb' line.\n5. CATALOG WINS. Locked v1.4 (tier-personas.ts): bare 99_000 / solo 399_000 / voice-starter 599_000 / library-full 799_000 (anchor strike 999_000 — the ONLY strikethrough on the page) / done-for-you 1_299_000 / enterprise contact-only. Hosting Rp 99rb/bulan every tier. month1Total 198rb/498rb/698rb/898rb/1,398jt. Map design tiers by FEATURE: Starter→solo, Pro→voice-starter, Premium(featured)→library-full, Ultimate→done-for-you, Enterprise→enterprise. Emit canonical slugs in checkout.html?plan=<slug> — NEVER ?plan=premium/ultimate/pro (they fall back/alias to the wrong tier and mis-sell). All other Rp figures in the design (27.5jt, 34jt, per-app strikes) are illustrative marketing — never wired.\n6. Preserve checkout slug contract + the CTA target/rel rule (external http/mailto→_blank; http(s)→rel=noopener; internal same-tab).\n7. BRAND: Bahasa Indonesia, 'kamu' (never 'Anda'/'lo'/'gue'); ZERO '!' in body copy (max 1 per caption); BANNED words: basically/just/literally/honestly/kind of/pretty much/revolutionary/disrupt/10x/game-changer/next-level; max 1 emoji per section.\n8. DEMO-HONESTY: app.jsx must NOT contain Sorted, emails, GST, 'PR #142', Auto-publish, 'Calendar update', 'confirmed dalam', 'Live di 6 platform', overnight, 'trending apa', Auto-monitor, 'otomatis ke OLX', 'kalender di-sync', 'Otomatis dibaca', '10×'; and MUST contain 'Pagi Briefing — dikirim otomatis' + 'Aku tidak mengirim apa pun tanpa kamu setujui'. Audit every ported demo bubble.\n9. No new npm packages or vendored libs. Ambient motion = CSS keyframes (in the one <style> block) or framer-motion. All JS (stage scaling, slider, count-ups, auto-scroll, node-sequence) goes in React useEffect — never DC's renderVals/_started pattern. Reproduce all visuals as inline SVG + CSS (the design has zero raster runtime assets); never ship /uploads or /screenshots.\n10. Work on branch landing/redesign-konten-port off landing/enhance-conversion. Run git from velorah/. Never commit to main. Final deliverable is a PR for founder review.\n\n=== WIRING CONTRACT (the 'nothing breaks' map — every design CTA → real target) ===\nNav: Mulai→#pricing; Beranda→#beranda, Kerja→#proses, Harga→#pricing, FAQ→#faq; logo→/. Hero: primary 'Aktifkan asisten kamu'→#pricing; ghost 'Konsultasi gratis'→https://cal.com/weuseai.agent/15min (_blank,noopener). Pricing: Starter→checkout.html?plan=solo; Pro→checkout.html?plan=voice-starter; Premium→checkout.html?plan=library-full; Ultimate→checkout.html?plan=done-for-you; Enterprise→mailto:kidnovell.richie@gmail.com?subject=Enterprise%20weuseai.agent (_blank) + https://wa.me/6282154902561?text=Halo%2C%20saya%20tertarik%20paket%20Enterprise (_blank,noreferrer); 'Lihat rincian biaya'→PriceBreakdownModal; modal footer→checkout.html?plan=library-full. Skills footer 'Lihat cara orang pakai'→use-cases.html; Coding early-access→https://wa.me/6282154902561?text=Halo%20Richie%2C%20mau%20early%20access%20Code%20Wizard%20agent%20di%20weuseai.agent (_blank). FAQ: 'Punya Pertanyaan?'→https://wa.me/6282154902561; 'Lihat semua pertanyaan'→/faq. Footer: Beranda/Cara Kerja/Harga→#beranda/#proses/#pricing; Privacy→/privacy; Terms→/terms; (+/refund-policy,/contact if rendered); support→mailto:support@weuseai.agent. Footer socials + halo@/sales@weuseai.id + phone + Discord pill = OMIT until founder confirms (NEVER ship href=\"#\" or an unverified mailto/tel). Persona deep-links (only if a persona section is kept): /persona?slug=<slug>, /checkout?persona=<slug>, caption 'harga →'→#pricing.\n\n=== EXECUTION (one section per iteration; do not advance until the current iteration is fully verified + committed) ===\nIteration 0 — Branch + baseline: from velorah/, `git checkout -b landing/redesign-konten-port`. Run `npm test` (record green) and `node scripts/build-landing.mjs` (confirm clean tree). Read the design HTML fully. Capture before-screenshots (1440/390) of current hero/pricing/faq/footer.\nIteration 1 — Foundation: extend the index.html Google Fonts <link> to add Plus Jakarta Sans (wght 400;500;600;700;800) alongside JetBrains Mono (keep Instrument Serif/Inter only if still referenced). Add the design's @keyframes + tokens + the prefers-reduced-motion reset to the single <style> block. No component changes.\nIterations 2–11 — Port sections IN THIS ORDER, each: (a) rebuild the section as a production React component (preserve section ids beranda/proses/pricing/faq), reproducing visuals as inline SVG+CSS, JS in useEffect; (b) wire every CTA per the WIRING CONTRACT; (c) audit copy against brand + demo-honesty locks; (d) `node scripts/build-landing.mjs`; (e) `npm test` green; (f) Playwright: serve static, 0 console errors, screenshot at 1440/768/390 vs the design reference, reduced-motion stops animation; (g) WIRING-CHECK: DOM-assert every CTA href/target/rel equals its target (pricing especially: Ultimate→done-for-you, never library-full fallback); for scarcity, mock the endpoint 200 (count renders) and 500/timeout (renders nothing); (h) commit [landing] <section>. Order: 2 Nav, 3 Hero, 4 Explore+calculator, 5 Memory&Persona, 6 Product Apps, 7 Skills, 8 Stepper, 9 Pricing (reuse existing logic; scarcity stays real; library-full featured + only 999 strike), 10 FAQ (reconcile head FAQPage JSON-LD; accordion via useState), 11 Footer (omit unverified socials/emails).\nIteration 12 — Cleanup: remove orphaned old components (only after confirming no references), set App() to the new section order with ids intact, prune unused keyframes/classes, final build + commit both artifacts.\nIteration 13 — Founder gate: full `npm test` green from clean tree; build byte-for-byte fresh; full-page Playwright pass (1440/768/390 + reduced-motion); walk the entire WIRING CONTRACT and confirm every target resolves (checkout.html?plan=<each slug> loads the right tier; /faq, use-cases.html, /privacy, /terms, /refund-policy, /contact 200; cal.com/wa.me/mailto carry correct target/rel). Open a PR (do NOT merge) with before/after screenshots, the wiring matrix, the pricing-reconciliation note, and FOUNDER-FLAG items: (i) unverified footer socials/emails/phone; (ii) brand wordmark split (.agent nav vs .id footer); (iii) the UNWIRED create-invoice 799→999 flip at paid_customers≥1000 — the page advertises 999 but create-invoice still charges 799; this must be wired before the 1000th sale or the claim breaks (out of scope for this migration — flag only).\n\n=== STOP CONDITIONS (halt and report, do not improvise) ===\n- Any gate goes red and the fix would require weakening a test → stop, report.\n- A design value contradicts the locked catalog and the resolution isn't 'catalog wins' → stop, report.\n- A CTA's real target is ambiguous or unverified (socials, new emails/phone) → omit + flag, do not invent.\n- You are about to touch main, checkout.html business logic, create-invoice, or any pricing number → stop, this migration is landing-visual + wiring only.\n\n=== DONE DEFINITION ===\nThe new design renders as the production landing on branch landing/redesign-konten-port; `npm test` is fully green; `node scripts/build-landing.mjs` produces no diff; index.html passes head-purity; every CTA in the WIRING CONTRACT resolves to its exact target with correct target/rel; the scarcity counter reads the real endpoint and hides on failure; the only strikethrough is library-full's 999 anchor; no demo-honesty/brand/pricing lock is violated; and a PR is open for founder review with the flag list. Nothing is merged to main.
