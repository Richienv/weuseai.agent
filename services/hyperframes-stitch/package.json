{
  "name": "@weuseai/hyperframes-stitch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HyperFrames stitch CLI — concatenates per-scene mp4 chunks from Runway/Pika using local ffmpeg, applying transitions per the storyboard recipe. Runs on customer VPS (or founder Mac). No external deps beyond ffmpeg (apt install ffmpeg / brew install ffmpeg).",
  "bin": {
    "hyperframes-stitch": "src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.spec.ts",
    "cli": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
